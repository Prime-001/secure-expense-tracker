spring:
  datasource:
    url: jdbc:mysql://localhost:3306/expense_db
    username: root
    password: Prem@2004

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
